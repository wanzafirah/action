streamlit==1.45.1

pandas>=2.2
requests>=2.31

# ASR — faster-whisper replaces openai-whisper (4x faster, same accuracy)
faster-whisper>=1.0.3

# Document extraction
pypdf>=4.0
python-docx>=1.1
openpyxl>=3.1

# Live transcription (optional — enables real-time mic recording + speaker diarization)
streamlit-webrtc>=0.47.0
deepgram-sdk>=3.2.0
av>=10.0.0
